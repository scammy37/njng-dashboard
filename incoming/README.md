# incoming/

Drop the latest NJNG bill PDF here (upload it via the GitHub web UI or mobile
app — no local setup needed), then ask Claude to process it. Claude parses the
bill, adds the entry to `data.json`, commits, and removes the PDF once it's in.

See the main README's "Adding a new bill" section for the full flow.
